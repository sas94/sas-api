pipeline {
   agent any
   triggers { 
     upstream(
      threshold: hudson.model.Result.SUCCESS,
      upstreamProjects: "fr-payment-batch-build"
	  )
	  }
	
	  stages {
	       stage('hello') {
		      steps {
			      echo 'hello world'
				 }
			}
		}
	}
